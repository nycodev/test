import { Tree } from "./tree-árvore.js";

const arvore = new Tree

arvore.insert(arvore.nodes, 23)
arvore.insert(arvore.nodes, 12)
arvore.insert(arvore.nodes, 4)
arvore.insert(arvore.nodes, 18)
arvore.insert(arvore.nodes, 1)
arvore.insert(arvore.nodes, 27)
arvore.insert(arvore.nodes, 25)

